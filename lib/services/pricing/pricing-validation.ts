import { DomainError } from '@/lib/domain/errors'
import type { ServerPricingRule } from '@/lib/domain/backend-types'
export function validatePricingRules(rules:ServerPricingRule[]){for(const rule of rules){if(!rule.days.length||rule.startTime<0||rule.startTime>=1440||rule.endTime<=0||rule.endTime>2880||rule.endTime===rule.startTime||rule.hourlyRate<0)throw new DomainError('VALIDATION_ERROR','A pricing band is malformed.',400)}for(let i=0;i<rules.length;i++)for(let j=i+1;j<rules.length;j++){const a=rules[i],b=rules[j];if(!a.days.some(day=>b.days.includes(day)))continue;const ae=a.endTime<=a.startTime?a.endTime+1440:a.endTime;const be=b.endTime<=b.startTime?b.endTime+1440:b.endTime;if(a.startTime<be&&ae>b.startTime)throw new DomainError('VALIDATION_ERROR','Pricing bands cannot overlap on the same day.',409)}return rules}
